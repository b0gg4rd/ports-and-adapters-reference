application.host = 0.0.0.0
application.port = 8040

database.url      = mongodb://${project.artifactId}-db:27017/payment?authSource=admin
database.username = root
database.password = root

docker.image.base = gcr.io/distroless/base-nossl-debian12

logger.root.level = INFO
logger.app.level  = DEBUG

functional-tests.server     = 'http://${project.artifactId}:${application.port}'
functional-tests.server.api = 'http://${project.artifactId}:${application.port}/api/v1'
